import type { LoaderContext } from 'webpack';
export interface Options {
    compatModuleNaming?: {
        rootDir: string;
        modulePrefix: string;
    };
}
export default function hbsLoader(this: LoaderContext<Options>, templateContent: string): string;
