import { Resolver } from './module-resolver';
export declare class ResolverLoader {
    #private;
    readonly appRoot: string;
    constructor(appRoot: string, watch?: boolean);
    close(): void;
    get resolver(): Resolver;
}
