import type { Variant } from './packager';
export declare class HTMLEntrypoint {
    private pathToVanillaApp;
    private rootURL;
    private publicAssetURL;
    filename: string;
    private dom;
    private placeholders;
    modules: string[];
    scripts: string[];
    styles: string[];
    constructor(pathToVanillaApp: string, rootURL: string, publicAssetURL: string, filename: string);
    private relativeToApp;
    private handledScripts;
    private handledStyles;
    render(stats: BundleSummary): string;
}
export interface BundleSummary {
    entrypoints: Map<string, Map<number, string[]>>;
    lazyBundles: Map<string, string[]>;
    variants: Variant[];
}
